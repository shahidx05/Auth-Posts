<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile</title>
    <link rel="stylesheet" href="css/style.css">
</head>

<body>
    <div class="head">
        <h1 class="h1">👋 Welcome, <%=user.name%>!</h1>
        <a href="/logout">Log Out</a>
    </div>

    <p style="margin: 10px 40px;" >Here you can manage your posts.</p>

    <div class="form">
        <!-- <h3>Create a New Post</h3> -->
        <form action="/post/create" method="post">
            <textarea placeholder="Write Somethig here... " name="content" id="textarea"></textarea>
            <button type="submit" id="Create">Create new post</button>
        </form>
    </div>

    <h2 style="font-size: 16px; font-family: sans-serif; margin: 0 40px; color: #717177; font-weight: 500; ">Your posts</h2>

    <% if (user.posts.length===0) { %>
        <h2 class="h2">No Posts Yet!</h2>
        <% } %>

            <% if (user.posts.length> 0) { %>
                <div class="postcontainer">
                    <%user.posts.slice().reverse().forEach(post=>{%>
                        <div class="post">
                            <p>
                                <%=post.content%>
                            </p>
                            <div class="btns">
                                <a class="delete" href="/post/delete/<%= post._id %>">Delete</a>
                                <a class="edit" href="/post/edit/<%= post._id %>">Edit</a>
                            </div>
                        </div>
                        <%})%>
                </div>
                <%}%>

</body>

</html>